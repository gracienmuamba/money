import React from 'react';
import * as ReactDOM from 'react-dom';
import './styles';
import { BrowserRouter } from 'react-router-dom';
import App from './views/app';
import { Analytics } from './common/analytics';
import { RMWCProvider } from '@rmwc/provider';
var renderApp = function (Component) {
    ReactDOM.render(React.createElement(RMWCProvider, null,
        React.createElement(BrowserRouter, { basename: process.env.PUBLIC_URL },
            React.createElement(Analytics, null),
            React.createElement(Component, { location: window.location.href }))), document.getElementById('root'));
};
// @ts-ignore
if (module.hot) {
    // @ts-ignore
    module.hot.accept(['./views/app'], function () { return renderApp(App); });
}
var init = function () {
    renderApp(App);
};
export default init;
